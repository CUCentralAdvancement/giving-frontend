{
  "name": "giving-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "yarn build && start-server-and-test 'yarn start' http://localhost:3000 'yarn cy:run'",
    "cypress:open": "cypress open",
    "cy:run": "cypress run --spec 'cypress/integration/real/**/*'"
  },
  "dependencies": {
    "next": "9.4.2",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  },
  "devDependencies": {
    "cypress": "^4.6.0",
    "start-server-and-test": "^1.11.0"
  }
}
